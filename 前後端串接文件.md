# Phoenix Crash 前端串接文件

## 1. 連線方式

WebSocket 連線：`ws://<host>:<port>/connect?token=<token>`

---

## 2. 訊息格式

### Client → Server

```json
{
  "op": "<opcode>",
  "data": {}
}
```

### Server → Client（成功）

```json
{
  "status": "ok",
  "type": "<opcode>",
  "data": {}
}
```

### Server → Client（錯誤）

```json
{
  "status": "error",
  "code": "<error_code>",
  "message": "<error_message>"
}
```

---

## 3. 進房流程

```
lobby.list → room.list → room.join → game.init（取得 gameState + balance）
```

### 3.1 `lobby.list`

Request:

```json
{
  "op": "lobby.list",
  "data": {
    "gameCode": "phoenix",
    "currency": "TWD"
  }
}
```

Response:

```json
{
  "type": "lobby.list",
  "data": {
    "lobbies": [
      {
        "lobbyId": "phoenix-default",
        "name": "Phoenix Crash",
        "gameCode": "phoenix",
        "currencyCode": "TWD",
        "minBet": 10,
        "maxBet": 10000,
        "maxPlayers": 200,
        "status": "active",
        "sortOrder": 100
      }
    ]
  },
  "status": "ok"
}
```

### 3.2 `room.list`

Request:

```json
{
  "op": "room.list",
  "data": {
    "lobbyId": "phoenix-default",
    "status": "open",
    "limit": 50
  }
}
```

Response:

```json
{
  "type": "room.list",
  "data": {
    "rooms": [
      {
        "roomId": "phoenix-room-002",
        "gameType": "Crash",
        "gameCode": "phoenix",
        "playerCount": 3,
        "maxPlayers": 200,
        "status": "open"
      }
    ]
  },
  "status": "ok"
}
```

### 3.3 `room.join`

Request:

```json
{
  "op": "room.join",
  "data": {
    "roomId": "phoenix-room-002"
  }
}
```

Response:

```json
{
  "type": "room.join",
  "data": {
    "roomId": "phoenix-room-002",
    "gameCode": "phoenix",
    "playerId": "player_001",
    "sessionId": "<session-id>",
    "minBet": 10,
    "maxBet": 10000
  },
  "status": "ok"
}
```

`room.join` 只回傳基本房間資訊，遊戲狀態和餘額需透過 `game.init` 取得。

### 3.4 `game.init`

進房後前端主動呼叫，取得遊戲設定與餘額。

Request:

```json
{
  "op": "game.init",
  "data": {
    "roomId": "phoenix-room-002"
  }
}
```

Response:

```json
{
  "type": "game.init",
  "data": {
    "roomId": "phoenix-room-002",
    "gameCode": "phoenix",
    "sessionId": "<session-id>",
    "minBet": 10,
    "maxBet": 10000,
    "gameInit": {
      "currencyCode": "TWD",
      "minBet": 10,
      "maxBet": 10000,
      "betLimitSource": "Merchant",
      "startMultiplier": 0.20,
      "maxMultiplier": 1500,
      "tickIntervalMs": 100,
      "multiplierCurve": [
        { "t": 0.0, "m": 0.20 },
        { "t": 0.4, "m": 0.40 },
        { "t": 0.8, "m": 0.65 },
        { "t": 1.3, "m": 0.85 },
        { "t": 1.8, "m": 0.95 },
        { "t": 2.5, "m": 1.20 },
        { "t": 3.6, "m": 2.00 },
        { "t": 5.2, "m": 3.00 },
        { "t": 7.6, "m": 5.00 },
        { "t": 11.2, "m": 8.00 },
        { "t": 16.2, "m": 12.00 },
        { "t": 23.0, "m": 18.00 },
        { "t": 32.0, "m": 26.00 },
        { "t": 44.0, "m": 40.00 },
        { "t": 59.5, "m": 60.00 },
        { "t": 77.0, "m": 100.00 },
        { "t": 95.0, "m": 200.00 },
        { "t": 112.5, "m": 400.00 },
        { "t": 128.5, "m": 800.00 },
        { "t": 142.0, "m": 1500.00 }
      ],
      "decimalPlaces": 2,
      "leaderboardLimit": 6,
      "maxBetsPerPlayer": 3,
      "betOptions": [1000, 2000, 3000]
    },
    "balance": {
      "playerId": "player_001",
      "currency": "TWD",
      "balanceUnits": "100000.00"
    }
  },
  "status": "ok"
}
```

重點：
- `gameInit.multiplierCurve`：倍數成長曲線，`t` 為秒數，`m` 為倍數，前端用此做插值動畫
- `gameInit.maxMultiplier`：最大崩潰點（1500），倍數不會超過此值
- `gameInit.maxBetsPerPlayer`：每回合最多下注數
- `gameInit.betOptions`：可用下注金額選項
- `balance`：玩家當前餘額，與 `game.balance` 回傳格式相同
- 若玩家在當前回合已有下注，server 會透過 push `crash.bet` 發送（見第 6 節）

---

## 4. 遊戲操作

### 4.1 `crash.bet` — 下注

僅在 `Betting` 階段可下注。

Request:

```json
{
  "op": "game.action",
  "data": {
    "roomId": "phoenix-room-002",
    "method": "crash.bet",
    "payload": {
      "betUnits": 1000,
      "autoCashoutMultiplier": 1.5,
      "betIndex": 0
    }
  }
}
```

| 欄位 | 說明 |
|------|------|
| `betUnits` | 下注金額 |
| `autoCashoutMultiplier` | 自動兌現倍數（到達時 server 自動兌現），可為 null |
| `betIndex` | 注單索引（0, 1, 2...，最多 `maxBetsPerPlayer - 1`） |

下注成功後，玩家會收到 targeted push：

```json
{
  "type": "game.action",
  "status": "ok",
  "data": {
    "method": "crash.bet",
    "payload": {
      "betId": "<bet-id>",
      "betIndex": 0,
      "betAmount": 1000,
      "autoCashoutMultiplier": 1.5,
      "balanceUnits": 99000.00
    }
  }
}
```

### 4.2 `crash.cashout` — 手動兌現

僅在 `Running` 階段可兌現。

Request:

```json
{
  "op": "game.action",
  "data": {
    "roomId": "phoenix-room-002",
    "method": "crash.cashout",
    "payload": {
      "betIndex": 0
    }
  }
}
```

Response:

```json
{
  "type": "game.action",
  "status": "ok",
  "data": {
    "method": "crash.cashout",
    "payload": {
      "betIndex": 0,
      "cashoutMultiplier": 2.50,
      "payoutGross": 2500.00,
      "serviceFee": 125.00,
      "payoutNet": 2375.00,
      "balanceUnits": 101375.00
    }
  }
}
```

Auto cashout 也透過相同的 `crash.cashout` push 通知玩家，格式一致。

**倍數精度**：
- Auto cashout：`cashoutMultiplier` 一定等於下注時設定的 `autoCashoutMultiplier`（精確值）
- 手動 cashout：`cashoutMultiplier` 為 server 處理當下的 `currentMultiplier`

### 4.3 `game.balance` — 查詢餘額

Request:

```json
{
  "op": "game.balance",
  "data": {
    "roomId": "phoenix-room-002"
  }
}
```

Response:

```json
{
  "type": "game.balance",
  "data": {
    "playerId": "player_001",
    "currency": "TWD",
    "balanceUnits": "99000.00"
  },
  "status": "ok"
}
```

### 4.4 `room.leave` — 離開房間

Request:

```json
{
  "op": "room.leave",
  "data": {
    "roomId": "phoenix-room-002"
  }
}
```

Response:

```json
{
  "type": "room.leave",
  "data": {
    "roomId": "phoenix-room-002",
    "roomClosed": false,
    "rooms": [
      {
        "roomId": "phoenix-room-002",
        "gameType": "Crash",
        "gameCode": "phoenix",
        "playerCount": 2,
        "maxPlayers": 200,
        "status": "open"
      }
    ]
  },
  "status": "ok"
}
```

- `roomClosed`：離開後該房間是否被關閉
- `rooms`：該 lobby 下的所有房間列表

---

## 5. 廣播事件

### 5.1 `room.round.state` — 回合狀態廣播

依狀態帶不同欄位，Running 階段每個 tick 廣播一次。

#### Betting（下注階段）

```json
{
  "type": "room.round.state",
  "data": {
    "roomId": "phoenix-room-002",
    "roundId": "<round-id>",
    "gameCode": "phoenix",
    "state": "Betting",
    "bettingCountdown": 5,
    "leaderboard": [
      {
        "playerId": "player_001",
        "totalBet": 3000,
        "betStatuses": [0, 0, 0],
        "rank": 1
      }
    ]
  },
  "status": "ok"
}
```

#### Running（進行中）

```json
{
  "type": "room.round.state",
  "data": {
    "roomId": "phoenix-room-002",
    "roundId": "<round-id>",
    "gameCode": "phoenix",
    "state": "Running",
    "currentMultiplier": 1.47,
    "runningElapsed": 2872,
    "leaderboard": [
      {
        "playerId": "player_001",
        "profit": 4410,
        "betStatuses": [1, 1, 0],
        "cashoutMultiplier": 2.35,
        "rank": 1
      }
    ]
  },
  "status": "ok"
}
```

#### Crashed（崩潰）

```json
{
  "type": "room.round.state",
  "data": {
    "roomId": "phoenix-room-002",
    "roundId": "<round-id>",
    "gameCode": "phoenix",
    "state": "Crashed",
    "crashPoint": 2.45,
    "crashedCountdown": 3,
    "runningElapsed": 5000,
    "leaderboard": [
      {
        "playerId": "player_001",
        "profit": 4410,
        "betStatuses": [1, 1, 2],
        "cashoutMultiplier": 2.35,
        "rank": 1
      }
    ]
  },
  "status": "ok"
}
```

#### Settled（結算完成）

Crashed 倒數結束後廣播，代表該回合結算完畢，即將開始下一回合。

```json
{
  "type": "room.round.ended",
  "data": {
    "roomId": "phoenix-room-002",
    "roundId": "20740f72b98e409796645b3884e8b6be",
    "gameCode": "phoenix",
    "kind": "Crash",
    "state": "Settled",
    "currentMultiplier": 3.5,
    "crashPoint": 3.5
  },
  "status": "ok"
}
```

---

#### Leaderboard 說明

Running / Crashed 的 leaderboard 格式相同：

| 欄位 | 型別 | 說明 |
|------|------|------|
| `playerId` | string | 玩家 ID |
| `profit` | decimal | 當前利潤 |
| `betStatuses` | int[] | 各注狀態：0=Pending, 1=CashedOut, 2=Lost |
| `cashoutMultiplier` | decimal? | 最後一筆已兌現的倍數，全未兌現為 null |
| `rank` | int | 排名 |

Betting 的 leaderboard 額外帶 `totalBet`，不帶 `profit` 和 `cashoutMultiplier`。

### 5.2 `room.bet.placed` — 全房下注廣播

```json
{
  "type": "room.bet.placed",
  "data": {
    "roomId": "phoenix-room-002",
    "roundId": "<round-id>",
    "gameCode": "phoenix",
    "betId": "<bet-id>",
    "playerId": "player_001",
    "betAmount": 1000,
    "betSeq": 1,
    "autoCashoutMultiplier": 1.5,
    "leaderboard": [...]
  },
  "status": "ok"
}
```

### 5.3 `room.round.started` — 新回合開始

```json
{
  "type": "room.round.started",
  "data": {
    "roomId": "phoenix-room-002",
    "roundId": "<round-id>",
    "gameCode": "phoenix",
    "roundSeq": 12345,
    "state": "Betting",
    "bettingCountdown": 10
  },
  "status": "ok"
}
```

### 5.4 `room.round.ended` — 回合結束

```json
{
  "type": "room.round.ended",
  "data": {
    "roomId": "phoenix-room-002",
    "roundId": "<round-id>",
    "gameCode": "phoenix",
    "kind": "Crash",
    "roundSeq": 12345,
    "state": "Settled",
    "currentMultiplier": 2.45,
    "crashPoint": 2.45
  },
  "status": "ok"
}
```

---

## 6. 斷線重連

玩家重連完成 `room.join` 或 `game.init` 後，若在當前回合有注單，server 會自動 push `crash.bet` 給該玩家：

```json
{
  "type": "game.action",
  "status": "ok",
  "data": {
    "method": "crash.bet",
    "payload": {
      "betId": "<bet-id>",
      "betIndex": 0,
      "betAmount": 1000,
      "autoCashoutMultiplier": 1.5,
      "balanceUnits": null
    }
  }
}
```

- `balanceUnits = null` 代表這是重連恢復，非新扣款
- 每筆注單各自 push 一次，前端用正常的 `crash.bet` handler 處理即可

---

## 7. 回合狀態機

```
Betting → Running → Crashed → Settled → (下一回合 Betting)
```

| 狀態 | 說明 | 前端可操作 |
|------|------|-----------|
| `Betting` | 下注階段，倒數計時中 | 下注 |
| `Running` | 倍數上升中 | 手動 cashout |
| `Crashed` | 已崩潰，顯示結果 | 無（等待） |
| `Settled` | 結算完成 | 無（自動進入下一回合） |

---

## 8. 常見錯誤碼

| 錯誤碼 | 說明 |
|--------|------|
| `missing_room_id` | 缺少 roomId |
| `missing_lobby_id` | 缺少 lobbyId |
| `room_not_found` | 房間不存在 |
| `room_full` | 房間已滿 |
| `not_joined_room` | 尚未加入房間 |
| `game_init_required` | 需要先 game.init |
| `betting_closed` | 非下注階段 |
| `insufficient_balance` | 餘額不足 |
| `invalid_json` | JSON 格式錯誤 |
| `unknown_op` | 未知操作碼 |
