# Phoenix Crash 前後端串接文件

## 1. 連線
WebSocket:

`ws://<host>:<port>/connect?token=<token>`

---

## 2. 通用封包

### 2.1 Client -> Server
```json
{
  "op": "<opcode>",
  "data": {}
}
```

### 2.2 Server -> Client（成功）
```json
{
  "status": "ok",
  "type": "<opcode>",
  "data": {}
}
```

### 2.3 Server -> Client（失敗）
```json
{
  "status": "error",
  "code": "<error_code>",
  "message": "<error_message>"
}
```

---

## 3. 進房流程

順序：`room.list -> room.join`（需要刷新初始化資料時再呼叫 `game.init`）

### 3.1 `room.list`
Request:
```json
{
  "op": "room.list",
  "data": {
    "status": "open",
    "limit": 50
  }
}
```

說明：
- `room.list` 目前只使用 `status`、`limit`（`lobbyId` 不使用）。

Response（前端收到的 model）：
```json
{
  "type": "room.list",
  "status": "ok",
  "data": {
    "rooms": [
      {
        "roomId": "phoenix-room-002",
        "gameType": "Crash",
        "gameCode": "phoenix",
        "playerCount": 3,
        "maxPlayers": 100,
        "status": "open"
      }
    ]
  }
}
```

`rooms[]` 欄位說明：
- `roomId`：房間 ID（後續 `room.join` 使用）。
- `gameType`：遊戲類型字串（目前 Phoenix 為 `Crash`）。
- `gameCode`：遊戲代碼（例如 `phoenix`）。
- `playerCount`：房間當前人數。
- `maxPlayers`：房間最大人數（可能為 `null`）。
- `status`：房間狀態（例如 `open`）。

### 3.2 `room.join`
Request:
```json
{
  "op": "room.join",
  "data": {
    "roomId": "phoenix-room-002"
  }
}
```

Response（重點欄位）：
```json
{
  "type": "room.join",
  "status": "ok",
  "data": {
    "roomId": "phoenix-room-002",
    "gameCode": "phoenix",
    "playerId": "player_001",
    "sessionId": "<session-id>",
    "minBet": 10,
    "maxBet": 10000,
    "gameState": {
      "currencyCode": "TWD",
      "minBet": 10,
      "maxBet": 10000,
      "betLimitSource": "Merchant",
      "startMultiplier": 0.2,
      "maxMultiplier": 1500,
      "tickIntervalMs": 100,
      "multiplierCurve": [
        { "t": 0.0, "m": 0.2 },
        { "t": 0.4, "m": 0.4 },
        { "t": 1.8, "m": 0.95 },
        { "t": 5.2, "m": 3.0 },
        { "t": 23.0, "m": 18.0 },
        { "t": 142.0, "m": 1500.0 }
      ],
      "crashDistribution": "inverse-power",
      "decimalPlaces": 2,
      "leaderboardLimit": 6,
      "maxBetsPerPlayer": 3,
      "betOptions": [1000, 2000, 3000],
      "roundHistory": [2.31, 1.47, 5.86, 1.02, 3.15],
      "balance": {
        "playerId": "player_001",
        "currency": "TWD",
        "balanceUnits": 100000.00
      }
    }
  }
}
```

補充：
- `balance` 和 `roundHistory` 由 Game.Server 在 `game.init` payload（`gameState`）內回傳，不在 `room.join` 外層。
- `roundHistory` 為崩潰點數字陣列（例如 `[2.31, 1.47, 5.86]`），最多近 100 場。
- `balance` 包含 `playerId`、`currency`、`balanceUnits`。

### 3.3 `game.init`
Request:
```json
{
  "op": "game.init",
  "data": {
    "roomId": "phoenix-room-002"
  }
}
```

Response（重點欄位）：
```json
{
  "type": "game.init",
  "data": {
    "roomId": "phoenix-room-002",
    "gameCode": "phoenix",
    "sessionId": "<session-id>",
    "minBet": 10,
    "maxBet": 10000,
    "gameInit": {
      "currencyCode": "TWD",
      "minBet": 10,
      "maxBet": 10000,
      "betLimitSource": "Merchant",
      "startMultiplier": 0.2,
      "maxMultiplier": 1500,
      "tickIntervalMs": 100,
      "multiplierCurve": [
        { "t": 0.0, "m": 0.2 },
        { "t": 0.4, "m": 0.4 },
        { "t": 1.8, "m": 0.95 },
        { "t": 5.2, "m": 3.0 },
        { "t": 23.0, "m": 18.0 },
        { "t": 142.0, "m": 1500.0 }
      ],
      "crashDistribution": "inverse-power",
      "decimalPlaces": 2,
      "leaderboardLimit": 6,
      "maxBetsPerPlayer": 3,
      "betOptions": [1000, 2000, 3000],
      "roundHistory": [2.31, 1.47, 5.86, 1.02, 3.15],
      "balance": {
        "playerId": "player_001",
        "currency": "TWD",
        "balanceUnits": 100000.00
      }
    }
  },
  "status": "ok"
}
```

---

## 4. 遊戲操作（`game.action`）

說明：`game.action` 目前統一使用 `data.instanceId` 指定房間實例（例如 `phoenix-room-002`），不使用 `data.roomId`。

### 4.1 `crash.state`
Request:
```json
{
  "op": "game.action",
  "data": {
    "instanceId": "phoenix-room-002",
    "method": "crash.state",
    "payload": {}
  }
}
```

Response（範例：Running）：
```json
{
  "type": "game.action",
  "status": "ok",
  "data": {
    "method": "crash.state",
    "payload": {
      "roundId": "4b8f5f7fd4f94a2cbf9874d2d22f5c77",
      "state": 1,
      "currentMultiplier": 2.35,
      "runningElapsed": 5200
    }
  }
}
```

### 4.2 `crash.reconnect`
Request:
```json
{
  "op": "game.action",
  "data": {
    "instanceId": "phoenix-room-002",
    "method": "crash.reconnect",
    "payload": {}
  }
}
```

Response（範例：Running，有一筆注單已兌現）：
```json
{
  "type": "game.action",
  "status": "ok",
  "data": {
    "method": "crash.reconnect",
    "payload": {
      "roundId": "4b8f5f7fd4f94a2cbf9874d2d22f5c77",
      "state": 1,
      "currentMultiplier": 2.35,
      "runningElapsed": 5200,
      "bets": [
        {
          "betAmount": 1000.0,
          "betIndex": 0,
          "autoCashoutMultiplier": 1.5
        },
        {
          "betAmount": 2000.0,
          "betIndex": 1
        }
      ],
      "cashouts": [
        {
          "betIndex": 0,
          "cashoutMultiplier": 1.5,
          "payoutGross": 1500.0,
          "serviceFee": 75.0,
          "payoutNet": 1425.0,
          "payout": 425.0
        }
      ]
    }
  }
}
```

說明：
- `state` 為 enum 數字：`0=Betting`、`1=Running`、`2=Crashed`、`3=Settled`。
- `currentMultiplier`：當前倍率（僅 Running 狀態帶此 key）。
- `runningElapsed`：運行經過毫秒數（Running / Crashed 才帶此 key）。
- `bets`：玩家當局所有注單，結構同 `crash.bet` response 的 `bets[]`。
- `cashouts`：玩家當局已兌現的注單，結構同 `crash.cashout` response 的 `cashouts[]`。

### 4.3 `crash.bet`（僅批次）
使用時機：`Betting`。

Request（必須使用 `payload.bets` 陣列）：
```json
{
  "op": "game.action",
  "data": {
    "instanceId": "phoenix-room-002",
    "method": "crash.bet",
    "payload": {
      "bets": [
        {
          "betUnits": 1000,
          "autoCashoutMultiplier": 1.5,
          "betIndex": 0
        },
        {
          "betUnits": 2000,
          "autoCashoutMultiplier": 2.0,
          "betIndex": 1
        }
      ]
    }
  }
}
```

Response（比照 `crash.cashout`：陣列 + 餘額）：
```json
{
  "type": "game.action",
  "status": "ok",
  "data": {
    "method": "crash.bet",
    "payload": {
      "bets": [
        {
          "betAmount": 1000.0,
          "betIndex": 0,
          "autoCashoutMultiplier": 1.5
        }
      ],
      "balanceUnits": 97000.0
    }
  }
}
```

說明：
- `payload.bets` 必填且必須是陣列。
- `betIndex` 範圍為 `0 .. maxBetsPerPlayer - 1`。
- `payload.bets[]` 只回傳 `betAmount`、`betIndex`、`autoCashoutMultiplier`。
- 同回合同 `betIndex` 重送會命中冪等，不會覆寫原下注。

### 4.4 `crash.cashout`（批次）
使用時機：`Running`。

Request:
```json
{
  "op": "game.action",
  "data": {
    "instanceId": "phoenix-room-002",
    "method": "crash.cashout",
    "payload": {
      "betIndexes": [0, 1, 2],
      "cashoutMultiplier": 2.5,
      "requestId": "cashout-batch-001"
    }
  }
}
```

Response：
```json
{
  "type": "game.action",
  "status": "ok",
  "data": {
    "method": "crash.cashout",
    "payload": {
      "cashouts": [
        {
          "betIndex": 0,
          "cashoutMultiplier": 2.5,
          "payoutGross": 2500.0,
          "serviceFee": 125.0,
          "payoutNet": 2375.0,
          "payout": 1375.0
        }
      ],
      "totalPayout": 1375.0,
      "balanceUnits": 101375.0
    }
  }
}
```

說明：
- `betIndexes` 必填且必須是陣列。
- `cashoutMultiplier`、`requestId` 為可選欄位。
- `payload.cashouts` 會回傳該玩家當局目前已 cashout（含 `CashoutPending`）清單。
- `payload.cashouts[].payout` 為單筆賠付，公式：`payoutNet - betAmount`。
- `payload.totalPayout` 為 `cashouts` 陣列總賠付（單筆賠付總和）。
- 「同一生命週期（同倍率）只推播一次」目前僅適用於 auto cashout 的 targeted push。

### 4.5 `crash.roundHistory`（近 100 場崩潰點）
使用時機：進房後初始化歷史圖表，或前端需要主動補拉歷史資料時。

Request:
```json
{
  "op": "game.action",
  "data": {
    "instanceId": "phoenix-room-002",
    "method": "crash.roundHistory",
    "payload": {
      "distributionRanges": [1, 2, 5, 20]
    }
  }
}
```

Response:
```json
{
  "type": "game.action",
  "status": "ok",
  "data": {
    "method": "crash.roundHistory",
    "payload": {
      "history": [
        2.31,
        1.47,
        5.86
      ],
      "distribution": [
        { "range": "0~1", "count": 12 },
        { "range": "1.01~2", "count": 28 },
        { "range": "2.01~5", "count": 36 },
        { "range": "5.01~20", "count": 18 },
        { "range": "20.01~", "count": 6 }
      ],
      "historyMaxCrashPoint": 128.4,
      "todayMaxCrashPoint": 11.49
    }
  }
}
```

說明：
- `history` 只回傳崩潰點數字陣列（`decimal[]`），最多近 100 場。
- `payload.distributionRanges` 為可選，格式為切割點陣列（需遞增且 > 0），例如 `[1,2,5,20]`。
- `distribution` 會依 `distributionRanges` 自動生成區間，回傳 `range/count`（例如 `0~1`、`20.01~`）。
- 若未傳 `distributionRanges`，後端預設使用 `[1,2,5,20]`。
- 資料來源邏輯為：先讀 Redis 歷史快取，若不足或不存在再 fallback 到 DB。
- `historyMaxCrashPoint` 為歷史最高崩潰點，`todayMaxCrashPoint` 為今日最高崩潰點。

---
## 5. 主要推播事件

### 5.1 `room.round.state`
回合狀態與倍率更新（`Betting / Running / Crashed`，包含 `leaderboard`）。

**Betting：**
```json
{
  "type": "room.round.state",
  "data": {
    "roomId": "phoenix-room-002",
    "roundId": "4b8f5f7fd4f94a2cbf9874d2d22f5c77",
    "gameCode": "phoenix",
    "state": "Betting",
    "bettingCountdown": 5,
    "leaderboard": [
      {
        "playerId": "player_001",
        "totalBet": 3000.0,
        "rank": 1
      }
    ]
  }
}
```

**Running：**
```json
{
  "type": "room.round.state",
  "data": {
    "roomId": "phoenix-room-002",
    "roundId": "4b8f5f7fd4f94a2cbf9874d2d22f5c77",
    "gameCode": "phoenix",
    "state": "Running",
    "currentMultiplier": 2.35,
    "runningElapsed": 5200,
    "leaderboard": [
      {
        "playerId": "player_001",
        "profit": 1375.0,
        "betStatuses": [1, 0, 0],
        "cashoutMultiplier": 1.5,
        "rank": 1
      }
    ]
  }
}
```

**Crashed：**
```json
{
  "type": "room.round.state",
  "data": {
    "roomId": "phoenix-room-002",
    "roundId": "4b8f5f7fd4f94a2cbf9874d2d22f5c77",
    "gameCode": "phoenix",
    "state": "Crashed",
    "crashPoint": 2.31,
    "crashedCountdown": 3,
    "runningElapsed": 5200,
    "leaderboard": [
      {
        "playerId": "player_001",
        "profit": 1375.0,
        "betStatuses": [1, 2, 2],
        "cashoutMultiplier": 1.5,
        "rank": 1
      }
    ]
  }
}
```

說明：
- `bettingCountdown`：下注倒數秒數。
- `currentMultiplier`：當前倍率（僅 Running）。
- `runningElapsed`：運行經過毫秒數（Running / Crashed）。
- `crashPoint`：崩潰點（僅 Crashed）。
- `crashedCountdown`：崩潰展示倒數秒數（僅 Crashed）。
- `leaderboard` 名次上限來自 DB 的 `crash_settings.leaderboard_limit`（`leaderboardLimit`）。
  - Betting：`[{ playerId, totalBet, rank }]`
  - Running / Crashed：`[{ playerId, profit, betStatuses, cashoutMultiplier, rank }]`
  - `betStatuses` 為 enum 數字陣列：`0=Pending`、`1=CashedOut`、`2=Lost`
- `room.round.state` 在 `Betting / Running / Crashed` 皆為每 tick 推播（即時更新前六名）。
- `Settled` 不會推送 `room.round.state`，改由 `room.round.ended` 推送。

### 5.2 `room.round.started`
新回合開始，payload 依當前狀態而定。

```json
{
  "type": "room.round.started",
  "data": {
    "roomId": "phoenix-room-002",
    "roundId": "4b8f5f7fd4f94a2cbf9874d2d22f5c77",
    "gameCode": "phoenix",
    "state": "Betting",
    "bettingCountdown": 5
  }
}
```

### 5.3 `room.bet.placed`
下注成功後的房內廣播（是否廣播受設定控制）。

```json
{
  "type": "room.bet.placed",
  "data": {
    "roomId": "phoenix-room-002",
    "roundId": "4b8f5f7fd4f94a2cbf9874d2d22f5c77",
    "gameCode": "phoenix",
    "betId": "bet_abc123",
    "playerId": "player_001",
    "betAmount": 1000.0,
    "betSeq": 0,
    "autoCashoutMultiplier": 1.5
  }
}
```

說明：
- `room.bet.placed` 不帶 `leaderboard`，排行榜統一由 `room.round.state`（tick）提供。

### 5.4 `room.cashout.done`
兌現成功後的房內廣播（受後端設定控制，非固定廣播；設定關閉時不會推送）。

```json
{
  "type": "room.cashout.done",
  "data": {
    "roomId": "phoenix-room-002",
    "roundId": "4b8f5f7fd4f94a2cbf9874d2d22f5c77",
    "gameCode": "phoenix",
    "playerId": "player_001",
    "cashouts": [
      {
        "betIndex": 0,
        "cashoutMultiplier": 2.5,
        "payoutGross": 2500.0,
        "serviceFee": 125.0,
        "payoutNet": 2375.0,
        "payout": 1375.0
      }
    ],
    "totalPayout": 1375.0,
    "requestId": "req_xyz789"
  }
}
```

### 5.5 `game.action` targeted push
推送給特定玩家（含同玩家多裝置），非房間廣播。

**crash.bet（下注結果）：**
```json
{
  "type": "game.action",
  "data": {
    "method": "crash.bet",
    "payload": {
      "bets": [
        {
          "betAmount": 1000.0,
          "betIndex": 0,
          "autoCashoutMultiplier": 1.5
        }
      ],
      "balanceUnits": 97000.0
    }
  }
}
```

**crash.cashout（兌現結果）：**
```json
{
  "type": "game.action",
  "data": {
    "method": "crash.cashout",
    "payload": {
      "cashouts": [
        {
          "betIndex": 0,
          "cashoutMultiplier": 2.5,
          "payoutGross": 2500.0,
          "serviceFee": 125.0,
          "payoutNet": 2375.0,
          "payout": 1375.0
        }
      ],
      "totalPayout": 1375.0,
      "balanceUnits": 101375.0
    }
  }
}
```

說明：
- `crash.bet` targeted push：下注成功時會推送。
- `crash.cashout` targeted push：目前主要用於 auto cashout 生命週期彙總推送與入房補發，不保證每次手動 cashout 都觸發單獨 targeted push。

### 5.6 `room.round.ended`（Settled）
回合結束事件（狀態為 `Settled`），用於通知前端本局正式結束（回合結束時一定會發送）。

```json
{
  "type": "room.round.ended",
  "data": {
    "roomId": "phoenix-room-002",
    "roundId": "4b8f5f7fd4f94a2cbf9874d2d22f5c77",
    "gameCode": "phoenix",
    "kind": "Crash",
    "state": "Settled",
    "currentMultiplier": 2.31,
    "crashPoint": 2.31
  }
}
```

### 4.6 `game.balance`
用途：主動查詢玩家即時餘額。通常 `room.join` 已帶回 `balance`，此接口用於需要手動刷新餘額時。

Request:
```json
{
  "op": "game.balance",
  "data": {
    "instanceId": "phoenix-room-002"
  }
}
```

Response:
```json
{
  "type": "game.balance",
  "status": "ok",
  "data": {
    "playerId": "player_001",
    "currency": "TWD",
    "balanceUnits": "99850.00"
  }
}
```

---

## 6. 重要串接規則

1. 推薦流程：`room.list -> room.join`。`room.join` 回包已帶 `gameState`（內含 `balance` / `roundHistory`），若需刷新可再呼叫 `game.init` 或 `game.balance`。
2. `game.action` 的 request 請使用 `data.instanceId`；若送 `data.roomId` 會被後端視為缺少 `instanceId` 而失敗。
3. `crash.bet` 現在不接受單筆物件格式，只接受 `payload.bets`。
4. `crash.bet` 與 `crash.cashout` 都會回傳 `balanceUnits`（可能因冪等命中而為 `null`）。
5. 前端若收到同 `betIndex` 重送結果，應視為同一注單結果而非新注。
6. `crash.bets` 目前後端仍支援，但前端正式串接建議使用 `crash.reconnect` / `crash.cashout` 回傳資料。
